[package]
name = "escrow_timelock"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "22.0.7"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
debug = false
panic = "abort" 